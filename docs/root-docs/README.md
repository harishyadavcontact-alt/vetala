# Root Docs

This folder contains root-level product and technical documents that were moved out of the repository root.

Use the root reading order:

1. `../../vetala_genesis.md` from repo root
2. `../../README.md` from repo root
3. files in this folder as supporting detail
